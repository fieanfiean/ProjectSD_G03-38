import 'package:flutter/material.dart';

class SearchTicketTabPage extends StatelessWidget {
  const SearchTicketTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('search'),
    );
  }
}



